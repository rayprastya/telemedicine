<%= form_with(model: meeting) do |form| %>
  <% if meeting.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(meeting.errors.count, "error") %> prohibited this meeting from being saved:</h2>

      <ul>
        <% meeting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tanggal, style: "display: block" %>
    <%= form.date_field :tanggal %>
  </div>

  <div>
    <%= form.label :dokter, style: "display: block" %>
    <%= form.text_field :doctor%>
  </div>
  <div>
    <%= form.hidden_field :account_id, value: Current.account.id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
